import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    height: 30,
  },
  day: {
    flex: 1,
    alignItems: 'center',
  },
  dayText: {
    fontSize: 14,
    color: '#666',
    lineHeight: 30,
  },
});
